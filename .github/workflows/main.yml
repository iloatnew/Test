name: Website Monitor

on:
  schedule:
    # Run every 5 minutes for website monitoring
    - cron: '*/5 * * * *'
    # Run daily at 7 AM UTC for test message
    - cron: '0 7 * * *'
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests
      
      - name: Create config file
        run: |
          cat > config.json << 'EOF'
          {
            "telegram_token": "${{ secrets.TELEGRAM_BOT_TOKEN }}",
            "telegram_chat_id": "${{ secrets.TELEGRAM_CHAT_ID }}",
            "websites": [
              {
                "name": "Brutal Assault Accommodation",
                "url": "https://brutalassault.cz/en/accommodation?show=ao",
                "check_for_keyword": "aamon",
                "extra_note": "🎪 New element detected with 'aamon' keyword!"
              }
            ]
          }
          EOF
      
      - name: Send Daily Test Message (7 AM UTC)
        if: github.event.schedule == '0 7 * * *' || github.event_name == 'workflow_dispatch'
        run: |
          python daily_test.py
      
      - name: Run Website Monitor (Every 5 minutes)
        if: github.event.schedule == '*/5 * * * *'
        run: |
          python website_monitor.py &
          sleep 60
          kill %1 2>/dev/null || true
